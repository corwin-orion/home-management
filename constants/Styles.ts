import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
  input: {
    width: '100%',
    padding: 10,
    borderRadius: 10,
    borderWidth: 1,
    borderColor: 'black',
  }
})